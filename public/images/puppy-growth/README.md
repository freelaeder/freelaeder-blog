# 小狗成长相册图片

`/puppy-growth` 页面会在开发启动或构建时自动扫描这个目录，图片名称和后缀不需要固定。

支持的格式：`.avif`、`.gif`、`.jpg`、`.jpeg`、`.jfif`、`.png`、`.svg`、`.webp`，后缀大小写均可。

需要明确分组时，请把图片分别放入 `group-1`、`group-2`、`group-3`。每组会按文件名自然排序；如果三个分组目录内都没有图片，则自动读取当前根目录，并按排序结果平均分为三组。

新增或替换图片后，重新运行开发服务或执行 `npm run build` 即可更新页面。
