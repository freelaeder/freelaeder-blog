---
title: "Claude Code 手册拆读 15：实用 Hook 案例集"
date: "2026-01-31T06:21:00+08:00"
slug: "698c2764"
tags:
  - "Claude Code"
  - "最佳实践"
  - "Hooks 自动化"
summary: "用可直接套用的 Hook 场景，把质量门禁、日志、通知和规范检查自动化。"
---

这篇笔记基于《Claude Code 最佳实践手册 v1.0.0》对应章节整理，保留原章节的核心结构，方便按主题快速复习与发布。

## 本节重点

- 这一节围绕主题建立完整认知框架。
- 优先保留能直接迁移到日常工作中的方法。
- 把概念、流程和实践建议整理成可复用笔记。

## 笔记整理

<p>六个完整可运行的 hook 案例，覆盖安全防护、代码质量、用户体验、合规审计四大维度。每个案例 包含完整的配置文件和 shell 脚本，可直接复制使用。</p>

<p>案例一：危险操作拦截（PreToolUse） 场景：阻止 rm -rf 、git push --force 、格式化磁盘等破坏性操作，防止 AI 误操作造成不可逆损失。</p>

<p>配置文件（.claude/settings.json ）：</p>

<p>{ { { } ] } ] } } 脚本（.claude/hooks/guard-bash.sh ）：</p>

<p># guard-bash.sh — 危险命令拦截 Hook # 危险模式列表（支持扩展） ) # 输出 JSON 决策：拒绝执行 --arg reason &quot;⚠️ 危险操作已被安全策略阻止：检测到模式 &#x27;$pattern&#x27;。如需执行，请手动在终端运行。&quot; \ &#x27;{ } }&#x27; # 无危险模式，正常放行 核心建议 将此脚本提交到项目仓库的 .claude/hooks/ 目录，团队所有成员的 Claude Code 会自动受到保护，无需各 自配置。</p>

<p>案例二：自动代码格式化（PostToolUse） 场景：Claude 写入或编辑代码文件后，自动运行 Prettier、ESLint、Black 等格式化工具，确保代码风格一致， 无需手动触发。</p>

<p>配置文件（.claude/settings.json ）：</p>

<p>{ { { } ] } ] } } # auto-format.sh — 自动格式化 Hook（async 模式，不阻塞主流程） # Prettier 格式化 性能说明：设置 &quot;async&quot;: true 后，格式化在后台进行，不影响 Claude Code 的响应速度。Claude 看到的 是格式化前的版本，但磁盘上的文件已经格式化完毕，下次 Claude 读取时会得到整洁的代码。</p>

<p>案例三：桌面通知推送（Notification） 场景：后台挂起 Claude Code 执行长任务时，任务需要确认或 Claude 空闲时，通过系统通知提醒用户回来查 看，避免长时间等待。</p>

<pre><code>配置文件（~/.claude/settings.json ，全局生效）：</code></pre>

<p>{ { { } ] } ] } } 脚本（~/.claude/hooks/notify.sh ）：</p>

<p># notify.sh — 跨平台桌面通知 Hook # 根据通知类型设置标题和紧急程度 TITLE=&quot;🔐 Claude 请求权限&quot; ;; TITLE=&quot;✅ Claude 等待输入&quot; ;; TITLE=&quot;💬 Claude 需要更多信息&quot; ;; *) ;; # macOS 系统通知 osascript -e &quot;display notification \&quot;$MESSAGE\&quot; with title \&quot;$TITLE\&quot; sound name \&quot;$SOUND\&quot; # Linux（需要 libnotify） # 降级方案：terminal-notifier（Homebrew） # 可选扩展：发送到 Slack Webhook（取消注释并设置 SLACK_WEBHOOK） 案例四：Stop 守门——任务完成验证 场景：要求 Claude 在宣布任务完成之前，必须先通过所有单元测试。防止&quot;我已完成&quot;实际上测试还是红的的情 况。</p>

<p>配置文件（.claude/settings.json ）：</p>

<p>{ { { } ] } ] } } # 在 Claude 停止前验证测试是否全部通过 # ⚠️ 关键：防止无限循环 # 如果这次 Stop 是由 hook 阻止后再次触发的，直接放行 # 检查是否有未提交的代码修改（有修改才需要验证） echo &quot;检测到代码修改，运行测试验证...&quot; &gt;&amp;2 # 根据项目类型选择测试命令 # 没有识别的测试框架，直接放行 # 阻止 Claude 停止，要求修复测试 &#x27;{ reason: (&quot;❌ 测试未全部通过，请修复以下错误后再完成任务：\n\n&quot; + $reason) }&#x27; echo &quot;✅ 所有测试通过&quot; &gt;&amp;2 # 测试通过或无需验证，允许 Claude 停止 注意 Stop hook 的 timeout 必须足够长（建议 60-120 秒），否则测试未完成 hook 就超时退出，会错误地放行 Claude 停止。</p>

<p>案例五：审计日志（PostToolUse 异步） 场景：合规要求记录所有 AI 对代码库的操作——写了哪些文件、执行了哪些命令、时间戳是多少。JSONL 格式 方便后续用 jq 分析。</p>

<p>配置文件（.claude/settings.json ）：</p>

<p>{ { { } ] } ] } } 脚本（.claude/hooks/audit-log.sh ）：</p>

<p># audit-log.sh — 操作审计日志 Hook（async 异步，不阻塞主流程） # 确保日志目录存在 # 提取关键字段 # 构造审计记录并追加到 JSONL 文件 &#x27;{ # 可选：每日生成摘要（每次写入后检查是否需要） # 使用锁文件避免并发写入</p>

<pre><code>[ $(( $(date +%s) - $(stat -f%m &quot;$LOCK_FILE&quot; 2&gt;/dev/null || echo 0) )) -gt 3600 ]; then</code></pre>

<p># 统计今日操作 WRITE_COUNT=$(grep &quot;\&quot;$TODAY&quot; &quot;$LOG_FILE&quot; 2&gt;/dev/null | grep &#x27;&quot;tool&quot;:&quot;Write&quot;&#x27; | wc -l | tr BASH_COUNT=$(grep &quot;\&quot;$TODAY&quot; &quot;$LOG_FILE&quot; 2&gt;/dev/null | grep &#x27;&quot;tool&quot;:&quot;Bash&quot;&#x27; | wc -l | tr -d 分析审计日志的常用查询：</p>

<p># 查看所有被写入的文件 # 统计各工具使用次数 # 查看某个会话的完整操作序列 案例六：配置变更告警（ConfigChange） 场景：企业环境中，检测并记录任何 settings.json 的修改，防止 AI 自行修改权限策略绕过安全规则。</p>

<pre><code>配置文件（~/.claude/settings.json ）：</code></pre>

<p>{ { { } ] } ] } } # audit-config.sh — 配置变更审计 Hook # 记录变更日志 # 对高风险变更（用户主动修改）触发桌面告警 # macOS 告警通知 with title \&quot;⚠️ Claude 配置变更警告\&quot; sound name \&quot;Basso\&quot;&quot; # 将变更内容快照保存 企业安全建议：在 managed-settings.json （IT 管理员控制）中设置 &quot;allowManagedHooksOnly&quot;:</p>

<p>true，可确保用户无法通过修改 settings.json 来绕过或删除安全 hook，形成&quot;自我保护&quot;的策略体系。</p>
