---
title: "Claude Code 手册拆读 13：settings.json 完全配置"
date: "2026-01-26T10:53:00+08:00"
slug: "claude-code-best-practices-13-settings-json"
tags:
  - "Claude Code"
  - "最佳实践"
  - "项目配置体系"
summary: "系统掌握模型、权限、Hooks、MCP 与沙盒等运行时配置的组织方式。"
---

这篇笔记基于《Claude Code 最佳实践手册 v1.0.0》对应章节整理，保留原章节的核心结构，方便按主题快速复习与发布。

## 本节重点

- 这一节围绕主题建立完整认知框架。
- 优先保留能直接迁移到日常工作中的方法。
- 把概念、流程和实践建议整理成可复用笔记。

## 笔记整理

<p>settings.json 是 Claude Code 的运行时控制中枢，涵盖模型选择、权限系统、沙盒规则、Hooks 生命 周期、MCP 服务器配置等 30+ 项配置。本节提供完整的配置速查表、权限系统 allow/deny/ask 规则 详解，以及生产环境就绪的完整配置示例。</p>

<p>配置文件位置与优先级 settings.json 支持四个位置，按优先级由低到高：</p>

<p>位置 | 作用域 | 可提交 git | 说明</p>

<pre><code>~/.claude/settings.json
全局（所有项目）
否
个人偏好和全局权限</code></pre>

<p>.claude/settings.json 项目（团队共享） 是团队共识的项目配置 .claude/settings.local.json 项目（个人私有） 否（自动 gitignore） 本地覆盖，不影响团队</p>

<p>企业托管 managed-settings.json | 组织全局 | 否 | IT 管理员配置，不可覆盖</p>

<p>30+ 配置项完整速查表</p>

<p>配置项 | 类型 | 说明 | 示例值</p>

<p>model string 覆盖默认模型 &quot;claude-sonnet-4-6&quot; effortLevel string 思考深度：low / language string Claude 响应语言 &quot;chinese&quot; permissions.allow array 自动允许的工具操作 列表 permissions.deny array 始终拒绝的操作列表 [&quot;Read(./.env)&quot;]</p>

<pre><code>permissions.ask
array
需要用户确认的操作
[&quot;Bash(git push *)&quot;]</code></pre>

<p>permissions.defaultMode string 默认权限模式 &quot;acceptEdits&quot; permissions.additionalDirectories array 允许访问的额外目录 [&quot;../shared-libs/&quot;] env object 每次会话注入的环境 变量 hooks object 生命周期 hook 配置见 Hooks 配置节 apiKeyHelper string 动态生成 API Key 的 脚本路径 cleanupPeriodDays number 会话历史保留天数 30 （0=禁用持久化） includeGitInstructions boolean 是否包含内置 git 工 作流提示 attribution.commit string git commit 署名文本 &quot;&quot; （&quot;&quot; 表示禁用） attribution.pr string PR 描述署名文本 &quot;&quot; disableAllHooks boolean 禁用所有 hook false allowedHttpHookUrls array HTTP hook 允许的 URL 白名单</p>

<p>配置项 | 类型 | 说明 | 示例值</p>

<p>sandbox.enabled boolean 启用沙盒隔离 false sandbox.autoAllowBashIfSandboxed boolean 沙盒内自动允许 sandbox.filesystem.denyRead array 禁止读取的路径列表 [&quot;~/.aws/credentials&quot;] sandbox.network.allowedDomains array 沙盒内允许访问的域 名 autoUpdatesChannel string 更新频道：stable / spinnerTipsEnabled boolean 显示旋转提示 tips false enableAllProjectMcpServers boolean 自动批准项目 .mcp.json 的所有 MCP 服务器 alwaysThinkingEnabled boolean 默认启用扩展思考模 式 plansDirectory string 计划文件存储目录 &quot;./plans&quot; outputStyle string 输出风格调整 &quot;Explanatory&quot; autoMemoryEnabled boolean 启用/禁用 Auto autoMemoryDirectory string 自定义 Auto Memory 存储路径 claudeMdExcludes array 排除特定 CLAUDE.md（仅 companyAnnouncements array 启动时展示的公司公 [&quot;欢迎使用内部 Claude&quot;] 告（企业级） feedbackSurveyRate number 质量调查出现概率 0 （0-1，0=禁用）</p>

<p>配置项 | 类型 | 说明 | 示例值</p>

<p>worktree.symlinkDirectories array worktree 中符号链接 的目录 权限系统：allow / deny / ask 详解 权限规则格式为 Tool 或 Tool(specifier)，求值顺序为 deny &gt; ask &gt; allow，第一个匹配规则生效：</p>

<p>{ ], ], ], } } 常用规则速查：</p>

<p>规则 | 匹配范围</p>

<p>Bash 所有 Bash 命令 Bash(npm run *) 以 npm run 开头的命令 Read(./.env) 读取 .env 文件 Read(./secrets/**) 读取 secrets 目录下所有文件 Edit(./src/**) 编辑 src 目录下所有文件 WebFetch(domain:example.com) 向 example.com 的请求 WebFetch 所有 WebFetch（常用于 deny） mcp__github__create_issue 特定 MCP 工具 defaultMode 可选值：</p>

<p>&quot;default&quot; — 每次操作都询问（最保守） &quot;acceptEdits&quot; — 自动接受文件编辑，其他操作询问（推荐日常开发） &quot;bypassPermissions&quot; — 跳过所有权限检查（仅受信任的自动化环境使用） 沙盒规则配置 沙盒模式为 Claude 的工具执行提供隔离环境，适合 CI/CD 和高安全要求场景：</p>

<p>{ }, } } } 路径前缀约定：/path 为绝对路径，~/path 相对 home 目录，./path 相对项目根目录（仅项目级配置有 效）。</p>

<p>完整生产配置示例 以下是一个适合中等规模团队项目的完整 .claude/settings.json 配置示例，覆盖权限、环境变量、Hooks 和沙盒：</p>

<p>{ ], ], ], }, }, { { } ] } ], { { } ] } ], { { } ] } ], { { } ] } ] }, } }, }, } 企业级管控配置 企业托管配置写入系统路径，对组织内所有用户强制生效，可限制用户级配置覆盖范围：</p>

<p>{ ] }, &quot;欢迎使用公司内部 Claude Code。请遵守信息安全规范，不得在对话中输入客户数据。&quot; ] } 注意 企业托管配置的 allowManagedPermissionRulesOnly: true 会使用户无法在自己的 settings.json 中添加额 外的 allow 规则，所有权限变更必须通过 IT 管理员。在部署前务必与开发团队充分沟通，避免影响正常工作 流。</p>

<p>配置策略建议：将团队共识的项目配置写入 .claude/settings.json 并提交 git；将个人调整（如禁用通 知、本地路径覆盖）写入 .claude/settings.local.json （自动 gitignore）；将跨项目的个人偏好写入</p>

<pre><code>~/.claude/settings.json 。三层分离，互不干扰。</code></pre>
